# Release Assets

Upload the files from the sibling local directory `battery-soh-rul-release-assets_<timestamp>/` to GitHub Releases, not to git.

Required files:

- `battery_work.tar.gz`
- `battery_judge.tar.gz`
- `SHA256SUMS`

Do not unpack `battery_judge.tar.gz` into the public repository unless hidden evaluation labels are intended to be public.
